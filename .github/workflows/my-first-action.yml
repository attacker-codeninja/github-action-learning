name: my-first-workflow-with-github-action

on: push

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: print hello world
        run: echo "Hello World"
